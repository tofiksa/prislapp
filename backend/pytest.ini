[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
pythonpath = .
markers =
    postgres: krever PostgreSQL via PRISLAPP_POSTGRES_TEST_URL; hoppes over uten den
